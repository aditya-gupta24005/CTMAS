# CTMAS — top-level dependencies
#
# IMPORTANT: install PyTorch FIRST with the correct CUDA index URL
# (see SETUP.md step 4). Do NOT pip-install torch via this file — it would
# overwrite a CUDA build with the CPU wheel. The packages below either
# require torch to be pre-installed (torch-geometric, opacus) or are
# torch-agnostic.

# Core scientific stack
numpy
pandas
scikit-learn
scipy

# Graph neural networks (assumes torch is already installed)
torch-geometric

# Federated learning
flwr>=1.15,<2.0

# Differential privacy (pinned — strict torch-version coupling)
opacus==1.5.2

# Explainability
shap

# API + dashboard
fastapi
uvicorn[standard]
websockets
python-multipart

# Utilities
tqdm
